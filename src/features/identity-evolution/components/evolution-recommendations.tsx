import { ArrowUpRight, Lightbulb } from "lucide-react";
import type { EvolutionRecommendation } from "@/features/identity-evolution/types";

export function EvolutionRecommendations({ recommendations }: { recommendations: EvolutionRecommendation[] }) {
  return <section aria-labelledby="evolution-recommendations-heading" className="glass rounded-2xl border border-white/[.09] p-5"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] tracking-[.16em] text-[#b6acff]">SMART RECOMMENDATIONS</p><h2 id="evolution-recommendations-heading" className="mt-2 text-xl font-medium text-white">What would make your Genome clearer?</h2><p className="mt-2 text-xs leading-5 text-slate-400">Recommendations name the evidence boundary and never claim an unavailable connector is active.</p></div><Lightbulb aria-hidden="true" className="size-5 text-[#b6acff]" /></div><div className="mt-5 grid gap-3">{recommendations.map((recommendation) => <article key={recommendation.id} className="rounded-xl border border-white/[.07] bg-black/[.1] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><h3 className="text-sm font-medium text-slate-100">{recommendation.title}</h3><span className={`rounded-full border px-2 py-1 font-mono text-[8px] tracking-[.11em] ${recommendation.status === "available_now" ? "border-cyan-300/20 bg-cyan-300/[.07] text-cyan-100" : "border-white/[.08] bg-white/[.025] text-slate-500"}`}>{recommendation.status === "available_now" ? "TEXT READY" : "FUTURE SUPPORT"}</span></div><p className="mt-2 text-xs leading-5 text-slate-400">{recommendation.detail}</p><ul className="mt-3 flex flex-wrap gap-2">{recommendation.suggestedSources.map((source) => <li key={source} className="inline-flex items-center gap-1 rounded-lg border border-white/[.07] bg-white/[.02] px-2 py-1 text-[10px] text-slate-400"><ArrowUpRight aria-hidden="true" className="size-3 text-slate-600" />{source}</li>)}</ul></article>)}</div></section>;
}
