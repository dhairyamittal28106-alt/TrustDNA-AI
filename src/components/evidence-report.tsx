import { FileText, ShieldAlert } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import type { InvestigationResult } from "@/features/judge/types";

export function EvidenceReport({ result }: { result: InvestigationResult }) { const highRisk = result.investigation.verdict === "impersonation_confirmed"; return <section className="mx-auto w-full max-w-7xl px-5 py-12 md:px-10"><Card className="glass border-white/10"><CardContent className="p-6 md:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#8f7bfa]/15 text-[#b4a7ff]"><FileText className="size-5" /></span><div><p className="font-medium text-white">Evidence report</p><p className="text-sm text-slate-500">Executive-ready forensic summary</p></div></div><span className="font-mono text-xs text-slate-500">{result.investigation.case_number}</span></div><div className="mt-8 grid gap-7 lg:grid-cols-3"><ReportSection title="Executive summary" body={highRisk ? "The submitted artifact is materially inconsistent with the selected Identity Genome. The evidence supports a high-risk impersonation finding." : "The submitted artifact is consistent with the selected Identity Genome based on the available deterministic evidence."} /><ReportSection title="Evidence" body={result.risk.rationale_codes.map((code) => code.replaceAll("_", " ")).join(" · ")} /><ReportSection title="Limitations" body="This MVP uses plain-text evidence and deterministic feature extraction. Voice, file metadata, and external-source evidence remain future modules." /></div><div className="mt-7 flex items-center gap-2 rounded-xl border border-amber-300/15 bg-amber-300/[.06] p-3 text-sm text-amber-100"><ShieldAlert className="size-4 shrink-0 text-amber-300" />Recommended action: preserve the Case File and verify the certificate before acting on the artifact.</div></CardContent></Card></section>; }
function ReportSection({ title, body }: { title: string; body: string }) { return <div><p className="text-xs font-medium tracking-[.12em] text-[#ada1ff]">{title.toUpperCase()}</p><p className="mt-3 text-sm leading-6 text-slate-400">{body}</p></div>; }
